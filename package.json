{
  "name": "busola-server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.ts",
    "prisma:pull": "bun prisma db pull",
    "prisma:migrate": "bun prisma migrate dev --name initial_migration"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "@supabase/supabase-js": "^2.52.1",
    "fastify": "^5.4.0",
    "prisma": "^6.12.0"
  }
}
